<%= form_with(model: [book, book_comment], local: false, id: "new_comment_#{book.id}") do |f| %>
  <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに", class: "w-100" %>
  <%= f.submit "送信" %>
<% end %>